using UnityEngine;

/// <summary>
/// 全局调试开关（临时使用）。
/// 在调试完成后可以改为 false 或移除此文件。
/// </summary>
public static class DebugSettings
{
    // 临时：走廊无敌开关，调试时设为 true
    public static bool CorridorGodMode = false;
}
